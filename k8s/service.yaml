apiVersion: v1

kind: Service

metadata:
  name: qa-task-manager-service

spec:
  type: NodePort

  selector:
    app: qa-task-manager

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080